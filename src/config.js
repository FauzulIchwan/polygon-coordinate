export const canvasSize = {
  width: 600,
  height: 400,
};

export const canvasConfig = {
  magnetRadius: 30,
};

export const canvasLineStyle = {
  fillStyle: 'red',
  strokeStyle: 'red',
  lineWidth: 1,
};

export const canvasDotStyle = {
  strokeStyle: 'red',
  lineWidth: 1,
};
